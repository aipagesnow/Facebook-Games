# Farm Flapper (Android)

**Game** — create the Play app as a **Game** (Arcade / Action). Cannot change later.

One-time import alongside the Instant Game. Wrap `games/farm-flapper/` with Capacitor. Stub FBInstant (share sheet + local scores). Leaderboard API is optional.

See `PORT-DECISION.md` (GO — operator requested both stores for this import only).
