# Word Streak Duels — Android workspace

Capacitor (or other wrapper) project root for the Play build.

- Listing copy: `android-listing.json`
- Store art: `store-assets/` (create files using the names in Play Console)
- Release artifact: `app-release.aab` (drop the signed bundle here)
- HTML5 source to wrap: `../../games/word-streak-duels/`

This folder is a studio workspace, not a generated Capacitor tree yet. Hand the info pack path to Grok to scaffold the real Android project here.
